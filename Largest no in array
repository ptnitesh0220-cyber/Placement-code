#include <stdio.h>

int main() {
	int n;
	scanf("%d",&n);
	int max=0,cur,max2=-1;
	for(int i=0;i<n;i++){
	    scanf("%d",&cur);
	    if(max<cur){
	        max2=max;
	        max=cur;
	    }
	    else if(cur>max2){
	        max2=cur;
	    }
	}
	printf("%d %d",max,max2);

}

